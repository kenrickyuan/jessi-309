<div class="navbar px-4 d-flex justify-content-between" style="height: 60px; background-color: #FE8180;">
  <div class="nav-item dropdown">
    <%= image_tag 'burger-menu.png', style: {height: '20', width: '22'}, class: "dropdown-toggle", id: "navbarDropdown", data: { toggle: "dropdown" }, 'aria-haspopup': true, 'aria-expanded': false %>
    <div class="dropdown-menu dropdown-menu-left" aria-labelledby="navbarDropdown">
      <% if !(@current.empty?) %>

        <div class="dropdown-item">
        Today
      </div>
      <div class="dropdown-item">
        <% @current.each do |current_event| %>
        <%= link_to current_event.title, event_path(current_event), class: "dropdown-link" %></div>
        <% end %>
        <% end %>
        <% if !(@pending.empty?) %>
        <div class="dropdown-item">
          Upcomming
        </div>
        <div class="dropdown-item">
          <% @pending.each do |pending_event| %>
          <%= link_to pending_event.title, event_path(pending_event), class: "dropdown-link" %>
          <% end %>
        </div>
        <% end %>
        <% if !(@past.empty?) %>
        <div class="dropdown-item">
          Past
        </div>
        <div class="dropdown-item">
          <% @past.each do |past_event| %>
          <%= link_to past_event.title, event_path(past_event), class: "dropdown-link" %>
          <% end %>
        </div>
        <% end %>

        <div class="dropdown-item">
        <%= link_to "Logout", destroy_user_session_path, method: :delete, class: "dropdown-link" %>
      </div>
      </div>
    </div>
    <%#= link_to image_tag('burger-menu.png', height: '20', width: '22'), events_path %>
    <h4 class="mb-0" style="font-family: Noto Sans; font-weight: bold; color: #472B62">jessi</h4>
    <%= link_to image_tag('plus.png', height: '22', width: '22'), new_event_path %>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <div class="nav-item dropdown">
        where am i
        <%= image_tag 'burger-menu.png', style: {height: '20', width: '22'}, class: "avatar dropdown-toggle", id: "navbarDropdown", data: { toggle: "dropdown" }, 'aria-haspopup': true, 'aria-expanded': false %>
        <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
          tetetete
          <% if !(@current.empty?) %>
          <div class="dropdown-item">
            <h2>Today</h2>
            <% @current.each do |current_event| %>
            <%= link_to current_event.title, event_path(current_event), class: "dropdown-link" %></div>
            <% end %>
            <% end %>
            <% if !(@pending.empty?) %>
            <div class="dropdown-item">
              <h2>Upcomming</h2>

              <% @pending.each do |pending_event| %>
              <h2><%= link_to pending_event.title, event_path(pending_event) %></h2>
              <% end %>
            </div>
            <% end %>
            <% if !(@past.empty?) %>
            <div class="dropdown-item">
              <h2>Past</h2>
              <% @past.each do |past_event| %>
              <h2><%= link_to past_event.title, event_path(past_event) %></h2>
              <% end %>
            </div>
            <% end %>
            <% if @events.empty? %>
            <%= link_to "Create an Event", new_event_path %>
            <% else %>
            <%= link_to "Create another Event", new_event_path %>
            <% end %>
            <%= link_to "Logout", destroy_user_session_path, method: :delete, class: "dropdown-link" %>
          </div>
        </div>
      </div>
    </div>
  </div>
