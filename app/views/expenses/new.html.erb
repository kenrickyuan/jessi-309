Who paid for what?

<%= simple_form_for [@event, @expense] do |f| %>
  <%= f.association :guest, collection: Guest.all %>
  <%= f.input :amount %>
  <%= f.input :description %>
  <%= f.submit "Add expense" %>
<% end %>
