<!-- We don't use this view -->
<div class="navbar px-4 d-flex justify-content-between sticky-top" style="height: 60px; background-color: #FE8180;">
  <%= link_to image_tag('back-arrow.png', height: '22', width: '20'),  event_guests_path(@event, @guest) %>
  <h5 class="mb-0" style="font-family: Noto Sans; font-weight: bold; color: #472B62"><%= @guest.name.capitalize %></h5>
  <div style="width: 22px"></div>
</div>

<div>
  <h1><%= @guest.name.capitalize %></h1>
  <% if @guest.description.nil? %>
  <%= link_to "Add Description", edit_event_guest_path(@event, @guest) %>
  <% else %>
  <p><%= @guest.description %></p>
  <% end %>

  <%= link_to "Edit Guest", edit_event_guest_path(@event, @guest)%>
</div>
